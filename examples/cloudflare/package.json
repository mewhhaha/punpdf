{
  "name": "@punpdf/example-cloudflare",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm --dir ../.. build",
    "deploy": "pnpm run build && wrangler deploy",
    "dev": "pnpm run build && wrangler dev",
    "typecheck": "wrangler types --check && tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@mewhhaha/punpdf": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "wrangler": "^4.112.0"
  }
}
